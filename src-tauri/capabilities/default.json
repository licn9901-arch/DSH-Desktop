{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local startup page has no privileged commands; remote Host URLs are not included",
  "windows": ["main"],
  "permissions": []
}
